<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jrbaena.penapp">

    <application
        android:name=".app.ClubApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Peñapp">
        <activity android:name=".app.home.FinancesActivity"></activity>
        <activity android:name=".app.home.EventsActivity" />
        <activity android:name=".app.home.OrdersActivity" />
        <activity android:name=".app.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.login.LoginActivity" />
        <activity android:name=".app.home.HomeActivity" />
    </application>

</manifest>